# Copiar a .env (no se versiona) o generarlo con scripts/init-env.sh / scripts/init-env.ps1.
POSTGRES_USER=audit
POSTGRES_PASSWORD=<cambiar>
RABBITMQ_USER=auditoria
RABBITMQ_PASSWORD=<cambiar>
# Clave HS256 de desarrollo, mínimo 32 caracteres.
JWT_CLAVE_FIRMA=<cambiar-minimo-32-caracteres>
